import React from 'react';
import { Text } from "react-native";

const ScheduledJobs = () => {
    return (
        <Text style={{ color: "#fff" }}>ScheduledJobs</Text>
    );
};

export default ScheduledJobs;